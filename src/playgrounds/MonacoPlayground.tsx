interface Props { topicId: string }
export default function MonacoPlayground({ topicId: _topicId }: Props) {
  return <div style={{ padding: 32, color: 'var(--text-muted)', textAlign: 'center' }}>Monaco playground coming soon…</div>
}
