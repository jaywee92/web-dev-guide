interface Props { topicId: string }
export default function VisualPlayground({ topicId: _topicId }: Props) {
  return <div style={{ padding: 32, color: 'var(--text-muted)', textAlign: 'center' }}>Visual playground coming soon…</div>
}
