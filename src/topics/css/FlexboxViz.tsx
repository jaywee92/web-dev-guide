interface Props { step: number; compact?: boolean }
export default function FlexboxViz({ step: _step, compact: _compact }: Props) {
  return <div style={{ textAlign: 'center', color: 'var(--text-muted)', padding: '40px 0' }}>Flexbox animation coming soon…</div>
}
